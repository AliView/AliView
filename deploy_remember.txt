Change version in:

pom
web-page index.html
version_history.txt
wikipedia AliView (after publish)

mvn clean install
copy_deploy

upload to GitHub:
// adding
git add --all
// removing
git add -u
// commit local
git commit -m "version 1.xx"
git push AliView master
username: AliView


move old versions on webserver to /old 

